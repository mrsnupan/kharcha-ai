<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config
  - Enforces TLS 1.2+ minimum (blocks TLS 1.0/1.1 downgrade attacks)
  - Blocks cleartext (HTTP) traffic in production
  - Allows cleartext only in debug builds (for local testing)
-->
<network-security-config>

    <!-- Production: HTTPS only, no cleartext -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds: allow localhost HTTP for testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

</network-security-config>
